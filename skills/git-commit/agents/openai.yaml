interface:
  display_name: "Git Commit"
  short_description: "Prepare and perform safe Git commits"
  default_prompt: "Use $git-commit to inspect my changes, propose a conventional commit, and commit only after explicit yes/y approval."
